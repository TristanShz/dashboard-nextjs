import React from "react"

type Props = {}

export function Navbar(props: Props) {
    return (
        <div
            className={
                "h-20 sticky top-0 bg-gray-50 dark:bg-darkMode-layer-2 shadow"
            }
        ></div>
    )
}
